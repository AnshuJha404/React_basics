{/*import React from 'react';

function HeroErrorHandler({heroname}) {
    if(heroname === "joker"){
        throw new Error ('Not a hero!')
    }
  return (
    <div>
      {heroname}
    </div>
  );
}

export default HeroErrorHandler;
 */}